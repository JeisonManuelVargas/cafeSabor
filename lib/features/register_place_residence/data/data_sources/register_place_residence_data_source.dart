
abstract class RegisterPlaceResidenceDataSource {
  /*Future<List<MovieModel>> getListMovies();*/
}

class RegisterPlaceResidenceDataSourceImpl implements RegisterPlaceResidenceDataSource {
/*  final FirebaseFirestore db;
  final String userCollection = 'users';

  RegisterPlaceResidenceDataSourceImpl({required this.db});

  @override
  Future<List<MovieModel>> getListMovies() async {
    try {
       final res = await AbstractApi().readData(urlSpecific: "movie/popular".stringToUri);
       List data = res.result['results'];
       final listMovie = data.map((e) => MovieModel.fromJson(e)).toList();
      return listMovie;
    } catch (e) {
      throw CreateAccountException(code: 'Error al cargar datos desde la API');
    }
  }*/
}
